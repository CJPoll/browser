logs/

# Project-local gem install (bundle config path vendor/bundle) — browser runs
# on system Ruby, gems vendored here so no root-owned system gem dir is touched
/.bundle/
/vendor/bundle/

# Cores land here: kernel.core_pattern is a bare "core" and `run` cd's to the
# project root before launching
core
core.*
